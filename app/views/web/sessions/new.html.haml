%h1 Log In
= simple_form_for :signin, url: session_path do |f|
	= f.input :email, input_html: {value: params[:signin] ? params[:signin][:email] : ''}
	= f.input :password
	= f.button :submit, 'Log In'